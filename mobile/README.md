# Mobile Client (Phase 7)

React Native + Expo staff client for billing and inventory on phones/tablets.

**Not started.** This folder is a placeholder per the build order in ARCHITECTURE.md.

Per product requirements:

- No PWA — mobile is native only
- Hardware integrations (Bluetooth thermal, Sunmi SDK) arrive with this phase
- Shares the same API and permission model as the web/desktop client
