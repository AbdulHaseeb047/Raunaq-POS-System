# Development: Vite proxies /api → localhost:3001
VITE_API_URL=/api
VITE_ADMIN_URL=http://localhost:5174

# Production (Vercel): set to full Railway backend URL (no trailing slash, no /api)
# VITE_API_URL=https://your-backend.up.railway.app
